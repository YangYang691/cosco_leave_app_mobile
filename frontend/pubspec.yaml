name: cosco_leave_app
description: COSCO Leave Application (auto-generated)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  sqflite: ^2.2.8+4
  path: ^1.8.4
  flutter_pdfview: ^1.2.7
  path_provider: ^2.0.17
  syncfusion_flutter_pdfviewer: 32.2.7
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/database/user.db
