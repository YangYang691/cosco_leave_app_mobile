cosco_leave_app
Generated by build_cosco_leave_app.ps1

How to run (frontend):
1. cd frontend
2. flutter pub get
3. Connect an Android emulator (or device) and run:
   flutter run

Note:
- The API base URL is currently http://10.0.2.2:5000 which maps to localhost when using Android emulator.
- If you run on a physical device, change baseUrl in lib/services/api_service.dart to your PC IP address.

Backend:
- No full backend included in this script (only placeholders). Implement endpoints expected by ApiService or point ApiService to your actual backend.
